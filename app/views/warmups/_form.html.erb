<%= form_for(@warmup) do |f| %>
  <% if @warmup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@warmup.errors.count, "error") %> prohibited this warmup from being saved:</h2>

      <ul>
      <% @warmup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="field form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: "form-control" %>
  </div>
  <div class="actions form-group">
    <%= f.submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
