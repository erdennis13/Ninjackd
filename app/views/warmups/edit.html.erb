<% if current_user.try(:admin?) %>
	<div class="panel panel-default">
		<div class="panel-body">
			<h1>Editing warmup</h1>

			<%= render 'form' %>
		</div>
		<div class="panel-footer">
			<%= link_to 'Show', @warmup %> |
			<%= link_to 'Back', warmups_path %>
		</div>
	</div>
<% else %>
	<%= render 'pages/unauth' %>
<% end %>