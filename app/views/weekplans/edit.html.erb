<% if current_user.try(:admin?) %>
	<div class="panel panel-default">
		<div class="panel-body">
			<h1>Editing weekplan</h1>

			<%= render 'form' %>
		</div>
		<div class="panel-footer">
			<%= link_to 'Show', @weekplan %> |
			<%= link_to 'Back', weekplans_path %>
		</div>
	</div>
<% else %>
	<%= render 'pages/unauth' %>
<% end %>
