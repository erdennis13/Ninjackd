<% if current_user.try(:admin?) %>
	<div class="panel panel-default">
		<div class="panel-body">
			<h1>New weeklyplan</h1>

			<%= render 'form' %>
		</div>
		<div class="panel-footer">
			<%= link_to 'Back', :back %>
		</div>
	</div>
<% else %>
	<%= render 'pages/unauth' %>
<% end %>