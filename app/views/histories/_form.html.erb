<%= form_for(@history) do |f| %>
  <% if @history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@history.errors.count, "error") %> prohibited this history from being saved:</h2>

      <ul>
      <% @history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :subscription_id %>
    <%= f.number_field :subscription_id, class: "form-control" %>
  </div>
  <div class="field form-group">
    <%= f.label :exercise_id %>
    <%= f.number_field :exercise_id, class: "form-control" %>
  </div>
  <div class="field form-group">
    <%= f.label :complete %>
    <%= f.check_box :complete, class: "form-control" %>
  </div>
  <div class="actions form-group">
    <%= f.submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>