<% if current_user.try(:append?) %>
	<div class="panel panel-default">
		<div class="panel-body">
			<h1>New workoutbit</h1>

			<%= render 'form' %>
		</div>
		<div class="panel-footer">
			<%= link_to 'Back', workoutbits_path %>
		</div>
	</div>
<% else %>
	<%= render 'pages/unauth' %>
<% end %>